spring:
  jpa:
    show-sql: false
    hibernate.ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect

location-client:
  url: http://location-client


outbox:
  delay: PT0.1S
  batch: 1000
